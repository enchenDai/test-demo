'use strict';

angular.module('DeepBlueAdminWebApp')
    .factory('Auth', function Auth($rootScope, $state, $q, Principal, AuthServerProvider, $localStorage, $sessionStorage) {
        return {

        };
    });
